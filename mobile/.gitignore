node_modules/
.expo/
dist/
*.log

# Native projects are generated by `expo prebuild` (EAS and local run:android
# regenerate them from app.json), so they must not be committed — a stale
# committed android/ breaks EAS autolinking ("No variants exist").
/android
/ios
