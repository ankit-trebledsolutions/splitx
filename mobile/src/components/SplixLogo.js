import React from 'react';
import Logo from '../assets/logo.svg';

// Official Splix logo, rendered straight from the design asset.
const SplixLogo = ({ size = 96 }) => <Logo width={size} height={size} />;

export default SplixLogo;
